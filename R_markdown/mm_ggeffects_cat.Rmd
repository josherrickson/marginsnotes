```{r}
summary(mod1 <- lm(y ~ class, data = m))
ggeffect(mod1, "class")
```

Note that we could have run that as `ggeffect(mod1, ~ class)`, with a formula.
However, when we later want to specify certain values of the predictors, we need
to use the character version, so I stick with it here.
